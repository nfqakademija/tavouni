<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand">Tavo uni</a>
        </div>
        <ul class="nav navbar-nav">
            {% for item in navigation_bar %}
                <li{% if item[0] == active %} class="active"{% endif%}>
                    <a href="{{ path(item[0]) }}">{{item[1]}}</a></li>
            {% endfor %}
            <li class="dropdown {% if 'lecturer_show_posts' == active %}active{% endif%}">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">Dėstomi dalykai
                    <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    {% for lecture in lectures %}
                        <li><a href="{{ path('lecturer_show_posts', {'subject_id': lecture.subject.id}) }}">{{ lecture.subject.name }}</a></li>
                    {% endfor %}
                </ul>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-log-out"></span>Atsijungti</a></li>
        </ul>
    </div>
</nav>